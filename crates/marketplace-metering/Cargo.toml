[package]
name = "marketplace-metering"
version = "0.1.6"
edition = "2021"
license = "Apache-2.0"
description = "AWS Marketplace metering payload helpers for PZDR Gateway"

[dependencies]
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "1"
